# Temporary: confirm the scraper now picks up today's draw via the homepage.
name: Probe 4d2ulive

on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install beautifulsoup4
      - name: Dry-run incremental since 2026-07-13 (should include today via homepage)
        working-directory: scrapers
        run: python3 scrape_4d2u.py --dry-run 2026-07-13 2026-07-15
